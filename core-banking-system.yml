server:
  port: 8081  # Let Spring Boot choose a random available port

spring:
  application:
    name: core-banking-system
  datasource:
    url: jdbc:mysql://localhost:3306/account
    username: root
    password: 9840115582  # Consider using environment variables or Spring Cloud Config for security
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
